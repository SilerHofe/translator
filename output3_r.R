# Сгенерировано транслятором JS→R  |  Лаба 3  |  Вариант 10

a1 <- 15
a2 <- 4
x <- 123.45
str <- "Привет мир"
sum <- (a1 + a2)
if (a1 > a2) {
  sum <- (a1 + a2)
} else {
  sum <- a2
}
square <- function() {
  r <- (x * x)
  return(r)
}
arr[1] <- 42
arr[a1 + 1 + 1] <- 100
