# Сгенерировано транслятором JS→R
# Лабораторная работа №3  |  Вариант 10
# ──────────────────────────────────────────────────

# Переменные: x
# Переменные: y
x <- 15
y <- 4
# Переменные: sum
# Переменные: mult
R1 <- x + y
sum <- R1
R2 <- y * x
mult <- R2
R3 <- sum > mult
if (!(R3)) goto(M1)
if (!("aloha")) goto_line_15
goto(M3)
R4 <- "Сумма:" + sum
R5 <- "Произведение:" + mult

# ──────────────────────────────────────────────────
# Таблица меток:
#   M2 → строка 15
