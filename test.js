// ============================================================
// Тестовая программа: полный срез языка JavaScript
// Вариант 10: Транслятор JS → R
// ============================================================

// ── 1. Числовые константы всех типов ──────────────────────

var n_int;
var n_fixed1;
var n_fixed2;
var n_fixed3;
var n_float1;
var n_float2;

n_int    = 42;
n_fixed1 = 3.14;
n_fixed2 = .5;
n_fixed3 = 25.;
n_float1 = 1.5e10;
n_float2 = 2.7E-3;

// ── 2. Строковые константы ────────────────────────────────

var s1;
var s2;

s1 = "Привет, мир!";
s2 = 'hello world';

// ── 3. Переменные и базовые типов ────────────────────────

var a;
var b;
var result;

a = 10;
b = 3;

// ── 4. Все арифметические операции ───────────────────────

result = a + b;
result = a - b;
result = a * b;
result = a / b;
result = a % b;


// ── 5. Составное выражение ────────────────────────────────

var x;
var y;
var z;

x = 5;
y = 2;
z = x * y + a / b;

// ── 6. Все операции сравнения ─────────────────────────────

var flag;


// ── 7. if без else ────────────────────────────────────────

var max;
max = a;

if (a > b) {
    max = a;
}

// ── 8. if-else ────────────────────────────────────────────

var min;

if (a < b) {
    min = a;
} else {
    min = b;
}

// ── 9. Вложенный if-else ──────────────────────────────────

var grade;
grade = 0;

if (result >= 5) {
    grade = 5;
} else {
    if (result >= 3) {
        grade = 4;
    } else {
        grade = 3;
    }
}

// ── 10. Цикл while ───────────────────────────────────────

var i;
var sum;

i = 1;
sum = 0;

while (i <= 10) {
    sum = sum + i;
    i = i + 1;
}

// ── 11. Функция без параметра ─────────────────────────────

function getSum() {
    var res;
    res = a + b;
    return res;
}

// ── 12. Функция с параметром ──────────────────────────────

function square(n) {
    var r;
    r = n * n;
    return r;
}

// ── 13. Функция с двумя параметрами ──────────────────────

function add(p, q) {
    var s;
    s = p + q;
    return s;
}

// ── 14. Функция с циклом внутри ───────────────────────────

function sumTo(n) {
    var s;
    var k;
    s = 0;
    k = 1;
    while (k <= n) {
        s = s + k;
        k = k + 1;
    }
    return s;
}

// ── 15. Вызов функций ────────────────────────────────────

var sq;
var total;
var total2;

sq     = square(a);
total  = add(a, b);
total2 = sumTo(10);

// ── 16. Одномерный массив ────────────────────────────────

var arr;

arr    = new array(5);
arr[0] = 10;
arr[1] = 20;
arr[2] = 30;
arr[3] = 40;
arr[4] = 50;

// ── 17. Обращение к массиву через переменную-индекс ──────

var val;
i = 0;
val    = arr[i];
arr[i] = arr[i] + 1;

// ── 18. Цикл с массивом ───────────────────────────────────

var acc;
acc = 0;
i = 0;

while (i < 5) {
    acc = acc + arr[i];
    i = i + 1;
}

// ── 19. Вложенный цикл ────────────────────────────────────

var count;
var j;

count = 0;
i = 1;
j = 1;

while (i <= 3) {
    j = 1;
    while (j <= i) {
        count = count + 1;
        j = j + 1;
    }
    i = i + 1;
}

// ── 20. Идентификаторы с подчёркиванием и цифрами ────────

var my_var1;
var _temp2;

my_var1 = 100;
_temp2  = my_var1 + 1;